"use client";
import { Layout } from "antd";

export default function Home() {
  const { Content } = Layout;

  return (
    <Layout className="h-screen">
      <Content></Content>
    </Layout>
  );
}
